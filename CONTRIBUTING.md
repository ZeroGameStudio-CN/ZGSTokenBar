# Contributing

Keep changes scoped to the native quota-bar product and preserve its local-first privacy model.

```powershell
npm install
npm test
npm run verify
```

Pull requests should summarize user-visible behavior, identify privacy or credential implications, add focused contracts, and update documentation when package names or provider behavior change.
