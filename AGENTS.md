# ZGSTokenBar 项目规则

只保留项目特有约定；构建、验收和账本细节见 [开发说明](docs/development.md)，不重复维护流程长文。

## 默认授权

实现/修改任务默认包含：本次提交、按仓库门禁合入并推送最新 `main`、同步本机主工作区、备份更新并重启本机 APP，以及清理本次可再生临时产物。无需逐阶段询问。只读咨询不执行这些写操作；公开发布、跨机器部署、数据迁移或清空数据另行授权。

## 完成标准

1. **同步与合并**：开工、提交前 `git fetch origin`，检查状态、分支及工作树，保留无关修改；只提交本次文件，遵守审核/CI，不硬重置、强推或绕过门禁。程序变更通过 `npm run verify` 后合入最新 `main`、推送；交付前再 fetch，将主工作区快进同步至 `origin/main`，确认该最终提交 CI 成功。只处理本次分支，不擅自合并他人工作。
2. **本机更新**：先定位现有部署。程序、依赖或资源变化时，从最终合入版本用全新的 `--artifacts-path` 和输出目录构建；备份旧版，正常退出目标实例，更新完整便携包并启动新版。纯文档变更不重打包，但仍确认运行版覆盖最新运行代码。
3. **防止覆盖**：更新程序不覆盖账本、设置、凭证或缓存。先核实真实数据目录及 AppX 重定向/Junction，禁止把同名路径当成同一文件；测试只用隔离数据根，不用旧副本、空数据或测试配置覆盖生产状态，不把两份账本直接相加。
4. **运行验收**：用 `ZGSTokenBar.Cli window inspect --json` 核对在用实例的 PID、路径、App/Core 模块 ID 与本次构建 DLL，以及 Token 基数、Radar 状态；外部 Job 查询须确认不受 Codex 宿主生命周期控制。不能只凭版本号、文件时间或磁盘哈希声称 APP 已更新。
5. **交付与清理**：确认本地/远端一致、无本次未提交或未合并工作，清理本次临时产物；不删活动数据、兼容链接或其他任务文件。必要备份说明保留范围和清理条件；策略拒绝时报告精确遗留，不换通道绕过。最终简报提交/CI、实际运行版和遗留/阻塞，全部验收完成才称“已完成”。
