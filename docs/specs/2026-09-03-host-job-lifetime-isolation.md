# 宿主 Job 生命周期隔离

- 状态：Completed（2026-09-07 运行版、数据统一与源码收尾）
- 最后更新：2026-09-07
- 基线：`d3c35ae4e1af429da689828c51b5fa71667b48b0`
- 设计与执行授权：已授权；用户要求修复 Codex 更新导致 ZGSTokenBar 退出
- 终端操作授权：用户于 2026-09-03 明确授权本机便携版部署与重启，并以“做完”授权既定范围的 Git 提交与推送；公开发布未请求

## 目标与非目标

### 2026-09-07 复发加固（以下条目取代原版对应决策）

用户报告关闭 Codex 后 TokenBar 再次退出，并要求修复；本次授权本地实现和非破坏性验证，不沿用历史提交/推送或部署授权。现场没有存活的 TokenBar 可追溯 Job 归属，应用崩溃事件未找到，部署文件与最后交付摘要一致；当前 Codex 命令所在 Job 的限制为 `0x2800`，原隔离探针可以脱离。因此本次修复可复现的保护缺口，不声称已经证明此次线上退出的唯一根因。

- 默认桌面实例处于任意 Job，或 Job 状态读取失败时尝试隔离。`QueryInformationJobObject(NULL)` 只查询最近一层 Job，其未设置终止标志不能证明外层 Job 安全。显式数据根仍完全跳过隔离及默认诊断写入。
- 保留 Explorer 父进程的隐藏解释器路线；失败时尝试 Windows 公开的 `CREATE_BREAKAWAY_FROM_JOB`，仅当系统允许时生效，不修改宿主 Job 限制，不新增服务、计划任务或 watchdog。
- 每次替代启动使用唯一的双事件握手。子进程确认自己不在任何 Job 中后报告就绪，收到原进程接受信号后才进入正常启动；原进程只有收到确认才退出。创建失败或 5 秒未确认时继续备用路线，全部失败则保留原实例并记录风险。迟到、失联或仍在 Job 中的替代实例退出，不争抢单实例互斥量。
- 标记只注入替代进程环境，替代实例入口消费并清除，避免后代继承；旧版固定值 `1` 不再能永久跳过隔离。
- 生产入口写入 `%LOCALAPPDATA%/ZGSTokenBar/diagnostics/startup-isolation.jsonl`，约 64 KiB 轮转并只保留一份 `.previous`。记录时间、PID、模块 ID、分支、Job 查询状态/标志和数字错误码，不记录命令行、环境、账号、路径或异常消息。日志写入失败不阻止启动；测试使用独立临时目录。
- 验收增加真实嵌套 Job 测试：外层随关闭终止、内层无终止标志；结束测试宿主后，绑定对照进程被终止，脱离后的替代进程仍能完成后续工作。不关闭 Codex，不启动/替换生产应用，不修改生产设置和账本。

恢复方式：恢复本次涉及的隔离实现、入口、测试和日志组件后重新构建；无数据迁移。诊断文件可单独移除，不影响用户设置和历史账本。

验证结果：聚焦 `--host-job-regression` 通过；完整 `npm run verify` 退出码 0，包含 108 项 Node 契约、113 项 .NET 测试、原生 HWND 生命周期、单文件 App、NativeAOT CLI、隔离插件验收和确定性截图。当前机器实际执行了嵌套 Job 宿主销毁测试，并未跳过。设置、Token 账本、Radar 状态及配额缓存的测试前后 SHA-256 全部一致。已自审全部任务差异，未部署、未启动生产实例、未提交/推送；本机便携版仍为此前的 `144F5A2E…D252533B`。Explorer 不可用且系统禁止直接 breakaway 时仍会保留宿主绑定实例并写入 `continuing-host-bound`，不能承诺绕过系统限制。

### 2026-09-07 本机替换与运行回读

用户随后明确授权“替换吧，确保用的是最新的”。只授权本机替换和启动；未请求 Git 提交/推送或公开发布。只读远端检查确认 `main` 为 `89961841e36ce1baed547c25ee4654d04742c7f0`，与本地 HEAD 一致，本次构建包含当前未提交的隔离修复。使用新的临时 artifacts/output 目录和仓库相同的 App/NativeAOT CLI publish 参数构建本机自用包；逐文件校验已安装 1.2.3 插件后保留其原文件/manifest，未重编或改变插件。便携目录、ZIP 和校验文件均已替换，ZIP 内四个文件与安装文件哈希一致。

- App SHA-256：`D5E56B48859DA3E77BAB3B31A14C3D8D12D4D3F9C4F77198A5EEF3C6CD2B53A7`。
- CLI SHA-256：`772EBB97E4E812F803062B92AC2C37178E61489FAB23683BF3A8524723FDFE0D`；ZIP SHA-256：`141E45D685D92C5FF8251045304C5864E035B49FCB261AD369FBD746A994898F`。
- 运行时 App/Core 模块 ID 与新构建 DLL 的元数据分别一致：`d39143f0-112c-4945-b0ad-288222fcbdd5` / `95c361fb-1b2d-4095-adac-2f2c18b97aa6`。
- 首次真实启动记录了最近一层 Job 限制 `0x800`（没有 KILL 标志），随后收到 `shell-confirmed`；替代 PID 49720 的外部探针为 `IsInJob=False`。这直接覆盖了旧版只检查最近一层 KILL 标志的遗漏，不等于证明此前所有退出的唯一原因。

运行验收同时发现了可复现的数据路径分流：Codex 包身份下的 `%APPDATA%/ZGSTokenBar` 实际映射到其 `LocalCache/Roaming/ZGSTokenBar`，这份索引保有 `369686331575` 历史基数；经 Explorer 原生父进程启动的无包身份进程读取真实 Roaming 目录，其另一份 Schema 8 索引没有历史基数。原生父进程的只读诊断证实两份索引及设置摘要不同；从 Codex 普通 Shell/COM 发起的读取仍可能看到虚拟化视图，不能把同名路径视为同一文件。这解释了本次新版模块正确而最初 UI 约 142B 的现象。

已正常退出首次实例，未合并或覆盖任何账本。随后通过同一 Explorer 父进程路线独立启动安装文件，并显式使用原有 `LocalCache/Roaming/ZGSTokenBar` 数据目录，保留原设置、历史基数和继续累计。最终 PID `38064`，窗口响应正常，外部 `IsProcessInJob` 查询成功且 `IsInJob=False`，实际 UI 累计 `371689099855`，设置 SHA-256 仍为 `C1F4F381B305F4237281156FEFC2B631A706BC051230BA7CA4D2A8D1BE04E533`。该实例已在原生启动阶段脱离宿主，不依赖显式数据根会跳过的自动隔离入口。

当前实例可独立运行，但未改写登录启动项、未迁移两份目录：后续普通启动仍可能读取不同账本。建议在单独确认后备份两份数据并统一独立存储和启动入口。没有实际关闭 Codex；存活保证限定为已验证解除的宿主 Job 连带终止路径，不覆盖程序自身崩溃、系统关机或其他显式终止。

旧完整包、替换前原账本/设置及新构建 DLL、脚本、插件输入与只读诊断保留在 `release/rollback-host-lifetime-20260907-205351/`。回退先正常退出新版，再恢复该目录中的旧完整包；本次无数据迁移，不自动回退正常累计的数据。清理临时构建目录的命令被执行策略拒绝，未换用其他通道删除；`C:/Users/2025/AppData/Local/Temp/zgstokenbar-lifetime-deploy-da61fcee69604fbeb392f3a136dbb855` 仍保留，证据已另存，确认无构建进程使用后可单独清理。回退目录保留至用户接受运行与后续数据统一结果。

### 2026-09-07 独立数据统一完成

用户同意继续备份迁移并把本机事项做好。所有实体路径操作均在已验证为无 AppX 包身份的原生桌面进程中执行，避免再次误读 Codex 虚拟视图；未修改应用统计代码、未重编已验证运行版、未改凭证、未触碰其他应用数据。

采用独立用户数据根 `C:/Users/2025/.zgstokenbar`。两条旧入口（真实 Roaming 目录和 Codex `LocalCache/Roaming` 目录）都改为指向它的 NTFS Junction，保留现有 App/CLI 默认路径契约，无需配置临时环境变量或 `--data-directory`。旧实体目录只作同卷改名，完整原件与另存快照均保留；没有递归删除用户数据。

选用迁移前正在正常使用的历史账本和设置：原生旧索引没有独有的计费会话，也没有更新的文件记录；其中 196 项旧累计较高，但属于不同累计口径，不能直接拼加。本次原样迁移主账本的 6390 条索引与 `369686331575` 历史基数，未重新计算历史、未合并旧累计。主目录 52 个文件、原生旧目录 46 个文件分别完整备份；迁移前后逐文件长度和 SHA-256 一致，并保留原访问控制。主设置摘要仍为 `C1F4F381B305F4237281156FEFC2B631A706BC051230BA7CA4D2A8D1BE04E533`。

验证结果：

- 原生侧文件句柄解析确认三条路径最终都为 `C:/Users/2025/.zgstokenbar/settings.json`；Codex 包身份侧三条路径的设置摘要也一致。一个额外的包身份句柄探针以 `-1` 退出且无结果，没有将它计作通过；随后用独立原生句柄解析、三路径内容摘要及实际默认启动验证完成交叉核对。
- 从 Codex 直接无参数启动新版，PID `93108` 正常显示约 371.7B，外部 Job 查询为 `IsInJob=False`，记录了 `0x800` 最近层 Job 下的 `shell-confirmed`。
- 正常退出该实例，再从现有开始菜单快捷方式无参数启动，最终 PID `95136`，窗口响应正常、实际 UI 累计 `371736954629`；App/Core 模块 ID 与部署验证相同，外部查询仍为 `IsInJob=False`。原账本和后续增量均保留。
- 原生 HKCU Run 值和开始菜单快捷方式都精确指向已校验的最新便携文件，无旧版本路径、watchdog 或临时数据参数。`openAtLogin=true`，Windows StartupApproved 中没有同名显式记录；没有改写该管理状态，也未实际注销/重启 Windows。
- 本轮仅迁移本机数据并补充记录，应用源代码未在已通过的完整门禁之后改变。没有关闭真实 Codex；存活证据仍限定于宿主 Job 连带终止路径，不能承诺应用永不崩溃。

操作脚本、双方完整快照、逐文件清单、预检和最终验证保留在 `release/rollback-data-unification-20260907-210851/`。两条旧实体目录保留为各自的 `.pre-unification-20260907-210851` 同级目录，不再是活动数据根。恢复前须正常退出应用并先保全独立根的最新增量；只移除已验证的兼容 Junction 本身，不递归删除其目标，也不得将旧快照直接覆盖正常累计后的新账本。备份与此前受策略限制的临时构建目录保持保留，未另走通道清理。

### 源码收尾授权与范围

用户最终明确要求“提交合并完”。提交前只读核对确认本地与远端 `main` 基线一致、无待合并 PR，分支未受保护且没有生效的分支规则；按仓库现有流程直接提交到 `main` 并推送，不使用强制推送或管理员绕过。范围仅为本次隔离实现、启动日志、回归测试、隐私说明和本记录共 8 个文件，`release/` 中的账本快照、程序备份、迁移脚本和构建文件不纳入 Git。

完整本地门禁与运行验收结果见上文；提交前应用和测试代码未在该门禁之后变更，另复核聚焦生命周期契约、静态检查及完整差异。提交后的 GitHub CI 按该不可变提交重新执行仓库完整门禁。该源码收尾不重新部署本机、不发布公开 Release；本机继续使用已核对模块 ID 和文件摘要的同一修复构建。

### 原版目标与实现记录

让从 Codex 等受 Windows Job 管理的宿主中启动的普通 ZGSTokenBar 实例脱离宿主的终止生命周期，使 Codex 更新或退出不会连带结束 TokenBar。

本次不恢复 watchdog，不新增服务、计划任务、安装器或崩溃自动恢复，不改变单实例、登录启动、更新检查、Provider 活跃检测、插件子进程监管和用户配置语义。

## 现状与目标行为

2026-09-03 11:23 的 AppX 部署事件显示 Codex 使用 `ForceTargetApplicationShutdownOption` 完成更新。现场进程探针确认 Codex 任务启动的命令位于同时设置 `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` 和 `JOB_OBJECT_LIMIT_BREAKAWAY_OK` 的 Windows Job 中；由该任务直接启动的 TokenBar 会继承 Job，因此在 Codex 更新关闭该 Job 时被系统一并终止。同期没有新的 ZGSTokenBar 应用崩溃事件，当前登录启动项仍指向有效的便携版路径。

普通数据根启动在读取配置、协调登录启动项或竞争单实例互斥量之前检查当前进程的 Job 限制。如果所在 Job 会随句柄关闭而终止成员，程序选取同一登录会话的 Windows Explorer，使用公开的 `PROC_THREAD_ATTRIBUTE_PARENT_PROCESS` 创建隐藏的系统命令解释器，再由该解释器普通启动替代实例；成功后当前实例立即正常返回。Windows 会让解释器及替代实例继承 Explorer 的 Job 归属而非 Codex 的嵌套 Job，因此替代实例继续现有启动流程时不再受宿主更新影响。

## 决策、约束与范围

- 仅对默认数据根的桌面实例启用生命周期隔离；显式 `--data-directory` 或 `ZGSTOKENBAR_DATA_DIRECTORY` 启动仍由测试或调用方拥有生命周期，且不触碰全局启动状态。
- 发现 `KILL_ON_JOB_CLOSE` 时才重新启动；普通进程、无终止语义的 Job、缺少同会话 Explorer 和 Win32 探针失败均原地继续，不让隔离功能阻止应用启动。
- 使用一次性进程环境标记防止替代实例在异常桌面环境中重新启动循环；标记只控制本次启动链，不写磁盘或用户配置。
- 生产入口只向解释器转发默认数据根下实际支持的 `--settings` / `settings` 参数，忽略本来就没有行为的未知参数；显式数据根不会走解释器。替代实例命令按 Windows 引号规则重建，并显式传递当前 Unicode 环境块和工作目录；不依赖框架构建中可能指向 DLL 的 `Environment.CommandLine`，不继承可继承句柄。环境内容只传给同一应用，不写入日志或磁盘；所有新建的进程与线程句柄均由启动实例关闭。
- `Program.cs` 只增加主启动流程调用；Win32 Job 检测与脱离启动封装在新的 App 内部组件中。回归覆盖加入 .NET 纯决策测试和生命周期静态契约。

## 失败、兼容与恢复

如果 Job 查询、Explorer 访问或替代实例创建失败，程序记录不含参数、路径或配置内容的 Trace 警告并继续当前实例；最坏行为退化为本次修复前的宿主绑定生命周期。登录启动的普通实例通常不在终止型 Job 中，不产生额外进程。显式隔离数据根保持原行为。

源码回滚可删除生命周期隔离组件及入口调用并重新构建便携版；不会迁移或改写用户数据。已部署构建为未签名的本机自用包，不作为公开发布资产。

## 实现与验证

1. 添加 Job 限制读取、脱离判定和 Explorer 父进程属性下的隐藏系统解释器重启组件，并在任何配置或全局状态访问前调用。
2. 添加决策回归和入口顺序/负向静态契约。
3. 运行聚焦 Node 契约、.NET 回归、Release 构建及完整 `npm run verify`；检查完整任务差异和 `git diff --check`。
4. 本机便携版已按用户授权部署并启动；通过最终进程 Job 状态验证生命周期隔离，不触发一次真实 Codex 更新作为破坏性验收，也不改写生产设置。

## 验收标准

1. 默认数据根实例处于设置 `KILL_ON_JOB_CLOSE` 的 Job 且同会话 Explorer 可用时，在配置加载、启动项协调和单实例判定前成功创建继承 Explorer Job 归属的等价实例，并结束原实例。
2. 不在 Job、Job 不会随关闭终止、已尝试替代启动或使用显式数据根时不重新启动。
3. Job 探针或创建失败不会阻止现有应用启动，也不会泄露命令行、路径或配置内容。
4. 原始参数、工作目录和单实例行为保持兼容；不恢复 watchdog，不新增持久系统组件，不改变插件子进程的 Job 监管。
5. 聚焦契约、.NET 回归、Release 构建、完整仓库门禁及差异检查全部退出码为 0；生产配置不被测试修改。

## 实现结果

- 已按设计在默认数据根启动入口加入宿主 Job 生命周期隔离；最终探针结果为 `querySucceeded=True`、`isInJob=False`、`limitFlags=0`。
- 聚焦生命周期契约 16/16、.NET 回归 110/110、完整 `npm run verify` 通过；完整门禁包含 107 项 Node 测试、Release 构建、原生 HWND 生命周期、单文件发布、NativeAOT CLI、隔离插件、确定性截图和差异检查。
- 未修改生产配置。已运行 `npm run dist` 部署未签名的本机自用便携版；`ZGSTokenBar.exe` SHA-256 为 `D2C80F8679C2E1B5A4CE82036E275D9B6DB6F6B12C5194723FE8F577B79A3F84`，便携 ZIP SHA-256 为 `8AC56A49F46E817F687CE95AF5E474DE559079C9148758D1F44563FB3DD5BE4B`，校验文件中的三项哈希均已复算匹配。
- 已启动部署目录中的 PID `140936`；进程响应正常，CLI `status --json` 返回 `running=true`、相同 PID、版本 `3.0.0` 及相同构建哈希。外部进程探针返回 `JobQuerySucceeded=True`、`IsInJob=False`，登录启动项仍精确指向该部署路径。
- 源码和本说明已由提交 `77b5816` 推送到 `origin/main`；公开发布不在本次范围。
